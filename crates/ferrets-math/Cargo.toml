[package]
name = "ferrets-math"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Deterministic RTS numeric primitives: fixed-point scalars, vectors, rectangles, and angles."

[dependencies]
fixed = { version = "1.31", features = ["serde"] }
serde.workspace = true

[dev-dependencies]
bcs.workspace = true
